**Date:** 21 May 2026
**Project:** SleepTracker PWA
**Module:** Code reviews, docstrings, final sweep, submission prep

**What I did:**
- Conducted peer code review on `routes/auth.py` with a classmate (see `CR-20260521 - Peer Review auth.py`). Issues raised tracked as GitHub Issues #1 and #2.
- Added Google-style docstrings to all classes (`User`, `SleepEntry`, `SleepGoal`) and all route functions, explaining parameters, return values, and security decisions.
- Removed all `print()` debug statements from `routes/auth.py`, `routes/sleep.py`, and `app.py`.
- Verified `git log --oneline` shows 11 commits with descriptive messages.
- Updated `README.md` with setup instructions, technology list, and feature list.
- Cloned `AlexandriaParkCS/pwa-flask-template` and created the official submission repository `2026-Y12-SE-daania-pwa-flask-application`. Transferred all code and verified the app runs cleanly from the cloned repo.

**Why I did it:**
- Docstrings are part of code quality (criteria 2.1) and serve as inline documentation  another developer reading `routes/sleep.py` can understand each function's purpose, parameters, and security reasoning without needing to read the full implementation.
- `print()` statements left in production code can expose sensitive information (usernames, session data) in server logs. Removing them before submission is a security and professionalism requirement.
- Meaningful commit messages (`"Add IDOR protection to edit and delete routes"` rather than `"update"`) document the evolution of the project and demonstrate iterative development (criteria 2.9).

**Challenges:**
- The submission repo clone required re-setting the `SECRET_KEY` in the config the `.env` file was excluded by `.gitignore`, which is correct security practice but meant remembering to configure it in the new repo.
- Confirming the app ran cleanly from the cloned repo caught one missing dependency in `requirements.txt` (`flask-wtf` had been installed but not added to the file). Fixed with `pip freeze > requirements.txt` before the final commit.

**Extra notes:**
- Complete portfolio (this vault).
- Push portfolio to `2026-Y12-SE-daania-pwa-project-portfolio`.
- Final submission by 8:30am Monday 25 May 2026.

